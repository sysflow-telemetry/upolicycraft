#!/usr/bin/env bash 

if [ "$#" -ne 2 ]; then
  echo "usage: $0 container-name chall"
  exit 1
fi

docker cp $1:/tmp/output.json plugins/mrm/resources/models/$2.json
docker cp $1:/tmp/output.dot plugins/mrm/resources/models/$2.dot

./bin/tweak-dot plugins/mrm/resources/models/$2.dot

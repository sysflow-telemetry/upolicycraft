#!/usr/bin/env bash

if [ "$#" -ne 1 ]; then
  echo "usage: $0 file"
  exit 1
fi

for i in $(ls layers)
do
  find ./layers/${i} -path "*$1" | xargs -L1 -I % sh -c 'file % | grep ELF' | awk '{print $1}' | cut -d ':' -f 1
done

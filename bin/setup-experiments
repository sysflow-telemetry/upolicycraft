#!/usr/bin/env bash

for program in $(ls ./experiments/redhat/); do
  experiment="./experiments/redhat/${program}"
  image="sysflowtelemetry/${program}"
  tarfile="${program}.tar"

  pushd $experiment
    docker build -t $image . 
    docker save $image > "../../../images/${tarfile}"
  popd
done

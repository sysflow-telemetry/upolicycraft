#!/usr/bin/env bash

if [ "$#" -ne 2 ]; then 
  echo "usage: $0 pipeline-config sf-trace"
  exit 1
fi

docker run -w /root/sf-processor/driver -v /Users/wdblair/work/sf-model/exit-demo21:/demo --rm -it sysflowtelemetry/mids:latest ./sfprocessor -log trace -config=$1 $2

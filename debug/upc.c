/*
 * These stubs are helpful for debugging. That is, if you want to see
 * data at some point in the program, a quick way to log it within Primus
 * is to pass it to one of these methods, which the uPolicyCraft plugin
 * recognizes and renders during micro execution.
 **/
void upc_log(char *msg) {
	return;
}

void upc_debug(void *p) {
	return;
}

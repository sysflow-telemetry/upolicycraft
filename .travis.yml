os: linux
dist: xenial
language: generic
sudo: required
services:
 - docker

script:
 - docker build -f docker/Dockerfile -t sysflowtelemetry/uids:latest .
 - docker build -f docker/Dockerfile.util -t sysflowtelemetry/uids-util:latest .
 - docker run -v $PWD/uIDS/artifacts/:/artifacts sysflowtelemetry/uids:latest /artifacts/fork/fork-argv _start "fork,/bin/bash,/bin/sh"

branches:
  only:
  - master

notifications:
  email: true
